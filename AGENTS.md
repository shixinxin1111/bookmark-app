# AGENTS.md

本文件记录项目长期有效的通用约束，会在后续会话和任务中作为上下文使用。

除非子目录另有更具体的说明，本文件适用于整个仓库。这里只放稳定、跨任务、跨模块的共识，不记录一次性任务说明、临时决策或过细的实现步骤。

## 项目定位

- 这是一个基于 Vite、React、TypeScript 和 Electron 的桌面端书签管理应用。
- 包名为 `bookmark-app`，主入口产物指向 `dist/main/main.js`。
- 项目以桌面端本地体验为主，前端界面运行在 Electron 渲染进程中。
- React 负责页面展示、交互状态和前端业务逻辑，Electron 负责桌面窗口、应用生命周期和系统能力桥接。
- 代码修改应服务书签管理应用本身，不引入与当前产品目标无关的复杂设计。

## 常用入口

- 本地开发入口：`pnpm dev`，会同时启动 Vite 开发服务和 Electron 应用。
- 类型检查入口：`pnpm typecheck`，覆盖渲染进程和 Electron 主进程 TypeScript 配置。
- 构建入口：`pnpm build`，先构建 Electron 主进程，再构建 Vite 渲染进程。

## 项目地图

- `src` 存放 React 渲染进程代码，承载页面结构、交互逻辑和前端状态。
- `src/main.tsx` 是渲染进程入口，负责挂载 React 应用。
- `src/app` 存放应用主体入口组件，只负责页面组合和少量页面级状态编排。
- `src/components` 存放应用主体的可复用 React 子组件，组件采用同名文件夹内 `index.tsx` 与 `index.module.css` 的组织方式。
- `src/hooks` 存放 React 状态与副作用封装，Hooks 文件使用 `.tsx` 后缀。
- `src/utils` 存放公共方法和纯业务工具函数。
- `src/constants` 存放稳定常量和选项配置。
- `src/types` 存放渲染进程内部共享类型。
- `src/style.css` 只存放真正全局的根节点样式和 Electron 交互兜底样式，组件样式优先使用 CSS Modules。
- `electron/main.ts` 是 Electron 主进程入口，负责窗口创建、应用生命周期和主进程能力。
- `electron/preload.ts` 是预加载脚本，负责向渲染进程暴露受控能力。
- `public` 存放无需源码编译处理的静态资源。
- `dist` 存放构建产物，不应手动编辑。

## 关键文件

- `package.json` 定义项目脚本、依赖和 Electron 主入口。
- `vite.config.ts` 定义渲染进程的 Vite 构建配置。
- `tsconfig.json` 覆盖渲染进程 TypeScript 配置，并通过 `@/*` 指向 `src/*`。
- `tsconfig.main.json` 覆盖 Electron 主进程和预加载脚本 TypeScript 配置。
- `index.html` 是 Vite 渲染进程 HTML 入口。
- `CLAUDE.md` 记录通用编码行为约束；本文件记录当前项目的结构和本地约定。

## 实现原则

- 保持渲染进程与 Electron 进程边界清晰，避免在 React 组件中直接依赖主进程实现细节。
- 业务逻辑应放在最贴近业务语义的位置，只有出现稳定复用需求时再抽象。
- 新增代码应优先沿用项目已有目录结构、命名方式和组织方式。
- `src` 下多单词目录和文件名使用小写短横线连接，例如 `hello-world`、`use-window-state.tsx`。
- 渲染进程内部跨目录导入优先使用 `@` 指向 `src`，减少深层相对路径。
- 修改应聚焦当前任务，避免顺手重构无关代码。
- 当新增稳定的公共能力时，应放在语义清晰的位置，并同步更新项目地图或关键文件说明。

## 代码质量

- 保持实现简洁直接，优先选择可读、可维护的写法。
- 组件和模块的职责应清晰，避免把无关状态或副作用塞进同一处。
- 重要类型、公共函数和关键边界应有必要注释，说明用途和约束。
- 注释应解释意图、约束或复杂逻辑，不要复述代码字面含义。
- 不引入未使用的依赖、配置、文件或占位代码。

## 验证原则

- 修改后应根据影响范围运行必要校验，并在结果中说明验证情况。
- 涉及界面行为时，应关注基础交互是否仍然可用。
- 涉及 Electron 主进程、预加载脚本或构建配置时，应额外确认桌面端启动和构建链路不受影响。

## 文档维护

- 日常开发中如果沉淀出通用能力、公共约束或跨模块约定，应及时补充到本文件中。
- 补充内容应保持简洁、稳定、可执行；不应写入个人临时偏好、一次性操作记录或已过期的初始化说明。
- 生成计划、说明、任务拆解等项目文档时，应优先使用中文标题和中文描述；文件名、命令、类型名、API 名称、代码片段和第三方专有名词可以保留英文。
